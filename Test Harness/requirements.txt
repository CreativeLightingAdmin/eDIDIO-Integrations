# eDIDIO integration test harness dependencies.
edidio_control_py>=0.4.0    # direct controller control (install -e ../../edidio_control_py for dev)
pymodbus==3.8.6             # Modbus gateway client (match the gateway's pin)
requests>=2.28             # REST gateway HTTP client
