"""eDIDIO MCP Server.

Exposes Control Freak eDIDIO lighting control as Model Context Protocol (MCP)
tools, so AI assistants (Claude, ChatGPT, etc.) can operate architectural
lighting — set levels, recall scenes, run SpektraPlus effects — via natural
language. Backed by the shared ``edidio_control_py`` engine.
"""

__version__ = "1.0.0"
