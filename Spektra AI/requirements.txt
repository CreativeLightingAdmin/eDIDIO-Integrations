# eDIDIO Spektra AI (MCP) dependencies.
mcp>=2.0              # Model Context Protocol SDK (MCPServer)
# Shared eDIDIO Python protocol engine (>=0.4.0 for authoring builders).
# Install locally in editable mode during development:
#   pip install -e ../../edidio_control_py
edidio_control_py>=0.4.0
