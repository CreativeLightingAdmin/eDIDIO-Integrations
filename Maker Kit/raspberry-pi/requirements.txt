# eDIDIO Raspberry Pi GPIO bridge dependencies.
gpiozero>=2.0         # GPIO input handling (Raspberry Pi)
PyYAML>=6.0
# Shared eDIDIO Python protocol engine (>=0.3.0). Install locally in dev:
#   pip install -e ../../../edidio_control_py
edidio_control_py>=0.4.0
# On a Pi you may also need a pin factory backend, e.g. RPi.GPIO or lgpio.
