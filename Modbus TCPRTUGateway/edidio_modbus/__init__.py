"""eDIDIO Modbus TCP/RTU Gateway.

Bridges a Modbus holding-register interface (for BMS/PLC integration) to the
eDIDIO lighting controller via the shared ``edidio_control_py`` engine.
"""

__version__ = "1.0.0"
